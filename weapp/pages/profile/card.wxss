/* pages/profile/card.wxss */
.container {
  min-height: 100vh;
  background: linear-gradient(180deg, #1a1a2e 0%, #f5f6fa 30%);
  padding-bottom: 60rpx;
}

/* 页面头部 */
.page-header {
  padding: 60rpx 40rpx 40rpx;
  text-align: center;
}

.page-title {
  display: block;
  font-size: 48rpx;
  font-weight: 700;
  color: #fff;
  margin-bottom: 16rpx;
}

.page-subtitle {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.7);
}

/* 名片预览区域 */
.card-preview-section {
  padding: 0 40rpx;
  margin-bottom: 40rpx;
}

.card-wrapper {
  background: #fff;
  border-radius: 24rpx;
  padding: 40rpx;
  box-shadow: 0 20rpx 60rpx rgba(0, 0, 0, 0.15);
}

.card-image {
  width: 100%;
  border-radius: 12rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.1);
}

.card-loading {
  height: 400rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.loading-spinner {
  width: 60rpx;
  height: 60rpx;
  border: 4rpx solid #f3f3f3;
  border-top: 4rpx solid #e94560;
  border-radius: 50%;
  animation: spin 1s linear infinite;
  margin-bottom: 20rpx;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.loading-text {
  font-size: 28rpx;
  color: #999;
}

.card-placeholder {
  height: 400rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: #f8f9fa;
  border-radius: 12rpx;
  border: 2rpx dashed #ddd;
}

.placeholder-icon {
  font-size: 80rpx;
  margin-bottom: 20rpx;
}

.placeholder-text {
  font-size: 28rpx;
  color: #999;
}

.card-hint {
  text-align: center;
  margin-top: 20rpx;
}

.hint-text {
  font-size: 24rpx;
  color: #999;
}

/* 操作按钮区域 */
.action-section {
  background: #fff;
  margin: 0 40rpx 30rpx;
  border-radius: 20rpx;
  padding: 40rpx;
}

.action-grid {
  display: flex;
  justify-content: space-around;
}

.action-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 30rpx 40rpx;
  background: #f8f9fa;
  border-radius: 16rpx;
  transition: all 0.2s;
  flex: 1;
  margin: 0 15rpx;
}

.action-item:first-child {
  margin-left: 0;
}

.action-item:last-child {
  margin-right: 0;
}

.action-item:active {
  background: #e9ecef;
  transform: scale(0.98);
}

.action-icon {
  width: 100rpx;
  height: 100rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 48rpx;
  margin-bottom: 16rpx;
}

.action-icon.save {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.action-icon.print {
  background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
}

.action-icon.share {
  background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
}

.action-text {
  font-size: 28rpx;
  color: #666;
  font-weight: 500;
}

.share-btn {
  background: transparent;
  padding: 0;
  margin: 0;
  line-height: inherit;
  border-radius: 0;
}

.share-btn::after {
  border: none;
}

/* 提示信息 */
.tips-section {
  margin: 0 40rpx;
  padding: 24rpx 30rpx;
  background: rgba(233, 69, 96, 0.1);
  border-radius: 16rpx;
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.tips-icon {
  font-size: 32rpx;
}

.tips-text {
  font-size: 24rpx;
  color: #e94560;
  line-height: 1.5;
}